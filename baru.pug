doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")

    // Menggabungkan CSS langsung di dalam file Pug
    style.
      body, html {
          margin: 0;
          padding: 0;
          width: 100%;
          height: 100%;
          display: flex;
          justify-content: center;
          align-items: center;
          font-family: 'Poppins', sans-serif;
          background: linear-gradient(180deg, #1d3e53 20%, #418ab9 85%);
      }
      .container {
          display: flex;
          max-width: 1200px;
          width: 100%;
          padding: 20px;
          box-sizing: border-box;
          color: #fff;
          gap: 40px;
          align-items: center;
      }
      .image-container {
          flex: 1;
          display: flex;
          justify-content: flex-start; // Mengatur agar gambar lebih ke kiri
          padding-right: 0; // Menghapus padding kanan
      }
      .image-icon {
          border-radius: 25px;
          width: 600px;
          height: 600px;
          object-fit: cover;
          margin-left: -40px; // Menambah margin negatif untuk menggeser lebih ke kiri
          margin-bottom: 20px; // Jarak bawah untuk memisahkan gambar dari form
      }
      .form-container {
          flex: 1;
          display: flex;
          flex-direction: column;
          align-items: center; // Mengatur agar form terletak di tengah
          padding-left: 20px; // Padding kiri untuk form
          margin-top: 20px; // Jarak atas untuk memisahkan form dari gambar
      }
      .create-account {
          font-size: 36px;
          margin-bottom: 20px;
          text-align: center; // Mengatur teks agar berada di tengah
      }
      .form-group {
          width: 100%;
          max-width: 400px;
          margin-bottom: 15px;
          display: flex;
          flex-direction: column;
      }
      .input-box {
          padding: 10px;
          border-radius: 10px;
          background-color: #fff;
          border: none;
          outline: none;
          color: #333;
          font-size: 16px;
      }
      .register-button {
          width: 100%;
          max-width: 400px;
          padding: 15px;
          background-color: #344ffb;
          color: #fff;
          font-size: 18px;
          font-weight: bold;
          text-align: center;
          border-radius: 20px;
          cursor: pointer;
          margin-top: 20px;
      }
      .extra-content {
          margin-top: 20px; // Mengatur jarak atas
          text-align: center; // Mengatur agar teks di tengah
          font-size: 16px;
      }

  body
    .container
      .image-container
        img.image-icon(alt="User Avatar" src="web-thecartel/image/Property 1=Variant2.jpg")
      
      .form-container
        h1.create-account Create Account

        .form-group
          label(for="first-name") First Name
          input#first-name.input-box(type="text" placeholder="Enter your first name")

        .form-group
          label(for="last-name") Last Name
          input#last-name.input-box(type="text" placeholder="Enter your last name")

        .form-group
          label(for="email") Email Address
          input#email.input-box(type="email" placeholder="Enter your email")

        .form-group
          label(for="password") Password
          input#password.input-box(type="password" placeholder="Enter your password")

        .register-button Register
        
        // Konten tambahan di bawah Create Account
        .extra-content
          p Sudah punya akun? 
          a(href="/login") Masuk di sini
          p Atau daftar dengan
          a(href="/google-auth") Google
          |  atau  
          a(href="/facebook-auth") Facebook
